# Local Server (Only for local usage)

Запуск подвійним кліком:
- `Start Local Server.command`

Що робить:
- запускає локальний backend
- відкриває браузер на `http://localhost:5050` (або PORT з `backend/.env`)

Цю папку не потрібно переносити на продакшн-сервер.
